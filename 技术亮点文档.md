# Ray社区 — 技术亮点文档

> 面试用途 · 技术深度展示 · Vue 3 + TypeScript 全栈前端项目

---

## 一、项目概述

Ray社区是一个从零搭建的潮流穿搭分享与服饰电商平台。项目采用 Vue 3 + TypeScript + Vite 技术栈，完整实现电商核心闭环（商品浏览 → 详情 → 购物车）和社区内容流（Feed → 发布 → 互动），同时支持会员体系、收藏、中英文切换和亮暗主题。

**开发周期**：2-3 天 | **代码量**：约 35 个模块文件 | **TypeScript 零编译错误**

---

## 二、架构设计

### 2.1 分层架构

```
View 层（页面视图）
    ↓ 使用
Component 层（复用组件）
    ↓ 调用
Composable 层（逻辑复用 hooks）
    ↓ 依赖
Store 层（Pinia 状态管理）
    ↓ 请求
API 层（Axios 封装）
    ↓ 访问
JSON Server（Mock REST API）
```

**设计原则**：
- 单向数据流：View → Composable → Store → API，数据流向清晰可追踪
- 关注点分离：视图、状态、网络请求各司其职
- 组件原子化：布局组件（Layout）、通用组件（Card）、业务组件（Badge）分层复用

### 2.2 状态管理设计（Pinia）

| Store | 设计要点 |
|-------|---------|
| `themeStore` | 管理 theme/locale/fontSize 三个维度，通过 `applyTheme()` 统一写入 DOM 和 localStorage |
| `userStore` | 登录/注册/登出 + 收藏 toggle + localStorage 持久化，token 和 user 分离存储 |
| `cartStore` | 增删改查 + totalPrice/totalCount 计算属性自动推导，颜色+尺码唯一键去重 |
| `memberStore` | currentLevel 从 userStore 派生，higherLevels 计算可升级等级，权益对比展示 |

**关键决策**：没有引入 Pinia 的 persist 插件，手写 `getStorage/setStorage` 封装实现精细化持久化控制。这展示了对 localStorage 的深度理解和避免过度依赖插件的工程意识。

---

## 三、核心技术实现

### 3.1 CSS Variables 主题系统

**挑战**：支持 3 个维度独立切换——颜色（亮/暗）、语言（中/英）、字号（小/中/大），且切换后全局生效并刷新保持。

**方案**：
1. 在 `:root` 定义全部 CSS 变量（颜色、间距、圆角、阴影、字号）
2. `html.dark` 选择器覆盖颜色变量
3. `html.font-small/font-large` 选择器覆盖字号变量
4. `themeStore.applyTheme()` 一次性同步 DOM class 和 localStorage

**优势**：纯 CSS 驱动，零 JS 运行时开销。Component 层通过 `var(--color-primary)` 引用，无需任何 JS 参与。改用 CSS 变量时，所有组件自动响应，无需 props 层层传递。

### 3.2 响应式国际化（轻量自研）

**挑战**：需要中英文切换但不想引入 vue-i18n（体量大、配置重）。

**方案**：
1. 语言包定义为 TS 对象（`zh-CN.ts` / `en-US.ts`），结构嵌套但扁平够用
2. Pinia `themeStore.t` 是一个 `computed`，根据当前 `locale` 返回对应语言包
3. 组件中通过 `useTheme().t` 获取翻译函数，Vue 响应式自动追踪，切换语言后全页面瞬间更新

**优势**：无额外依赖，TypeScript 自动补全翻译 key，零配置。

### 3.3 路由导航守卫

**场景**：已登录用户访问登录页 → 重定向首页；未登录用户访问需认证页面 → 弹提醒并暂存目标路由，登录后自动跳回。

**实现**：
- `router.beforeEach` 中检查 `meta.requiresAuth` 和 `meta.guestOnly`
- 未登录拦截时，通过 `query.redirect` 暂存当前路径
- 登录成功后读取 `route.query.redirect` 跳回

### 3.4 购物车状态管理

**设计**：
- 商品按 `productId + color + size` 组合作为唯一标识
- `totalPrice` 和 `totalCount` 使用 Pinia `computed` 自动计算
- 所有变更通过 `persist()` 写入 localStorage，刷新不丢失
- 加入购物车时使用 `ElMessage.success` 即时反馈

---

## 四、性能优化

### 4.1 数据层

- **并发请求**：首页 4 个 API（Banner/热门/新品/社区）通过 `Promise.all` 并发发起，减少总等待时间
- **懒加载路由**：所有页面组件使用 `() => import()` 动态导入，Vite 自动代码分割，首屏只加载当前页面 JS
- **图片懒加载**：商品卡片 `<img loading="lazy">`，减少首屏带宽

### 4.2 渲染层

- **骨架屏**：LoadingSkeleton 组件支持 card/list/detail 三种模式，shimmer 动画丝滑
- **过渡动画**：页面切换使用 Vue `<transition>` fade 动画，Router 级统一管理
- **v-if 计算属性缓存**：`isCollected` 等判断逻辑通过 computed 缓存，避免模板中重复计算

### 4.3 构建层

- Vite 原生 ESM 开发服务器，启动 < 300ms
- 生产构建 Tree Shaking 自动移除未使用代码

---

## 五、工程化实践

### 5.1 TypeScript 全量覆盖

- 所有 API 返回值类型化（`request.get<Product[]>()`）
- 组件 Props 和 Emits 类型约束
- Store state 类型从泛型自动推导
- 工具函数的泛型封装（`getStorage<T>`）

### 5.2 组件设计模式

- **插槽模式**：EmptyState 组件通过 `<slot>` 暴露操作按钮和文案，父组件控制行为
- **组合式函数**：`useAuth` / `useTheme` 封装 Pinia store 的常用操作，降低组件耦合
- **条件渲染守卫**：发布穿搭按钮点击后先调用 `requireAuth()` 做鉴权拦截

### 5.3 错误处理

- Axios 拦截器统一捕获 HTTP 错误，`ElMessage.error` 提示
- 网络异常时 try-catch 兜底，不影响页面渲染
- 收藏操作乐观更新：先改 UI 再发请求，失败时回滚

---

## 六、技术决策总结

| 决策点 | 方案 | 原因 |
|--------|------|------|
| 状态管理 | Pinia | Vue 3 官方推荐，TS 友好 |
| 国际��� | 自研轻量方案 | 无需额外依赖，可控 |
| 主题系统 | CSS Variables | 性能最优，零 JS 开销 |
| Mock 方案 | JSON Server | REST API 模拟，GET/POST/PATCH 完整 |
| UI 库 | Element Plus | 组件丰富，TypeScript 支持好 |
| 路由懒加载 | 动态 import | 天然代码分割 |
| 购物车存储 | localStorage | 刷新不丢，无需后端 |

---

## 七、项目运行

```bash
npm install          # 安装依赖
npm run dev          # 启动开发环境（Vite:3000 + JSON Server:3001）
```

浏览器访问 `http://localhost:3000` 即可体验完整功能。

---

## 八、功能清单

| 状态 | 模块 | 关键技术 |
|------|------|---------|
| ✅ | 首页 | Banner轮播、并行数据加载、骨架屏 |
| ✅ | 商品列表 | 分类筛选、多维度排序、URL同步 |
| ✅ | 商品详情 | 颜色/尺码选择、收藏、加入购物车 |
| ✅ | 购物车 | 增删改查、computed总价、持久化 |
| ✅ | 穿搭社区 | Feed流、点赞、加载更多 |
| ✅ | 发布穿搭 | 图片+文字+标签（登录鉴权） |
| ✅ | 登录/注册 | 手机号登录、注册、路由守卫 |
| ✅ | 个人中心 | 用户信息、功能入口、会员标识 |
| ✅ | 我的收藏 | 已收藏商品列表 |
| ✅ | 会员中心 | 等级展示、权益对比表、升级 |
| ✅ | 设置 | 中英文切换、亮暗主题、字号调节 |
